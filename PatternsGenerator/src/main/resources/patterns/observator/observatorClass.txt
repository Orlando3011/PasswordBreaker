%creationInfo%
public class %observator% implements %observator%Interface
{
	private %stateType% %state%;
	private %observated% %observatedObject%;

	public %observator%(%observated% %observated%Object) {
		this.%observated%Object = %observated%Object;
	}

    %updateInfo%
	public void %update%() {
		%observatedState% %observatedState%Object = %observated%.get%observatedState%Object();
		this.%changeState%(%observatedStateObject%);
	}

    %changeStateInfo%
    private void %changeState%(%observatedState% %observatedState%Object) {
        %emptyFunction%
        return null;
    }
}