%creationInfo%
import java.util.List;
import java.util.ArrayList;

public class %observated% implements %observated%Interface
{
	protected List<%observator%> %observator%s = new ArrayList<%obserwator%>();
	private %observatedState% %observatedState%Object;

    %crudInfo%
	public void add%observated%(%observator% %observator%Object) {
		%observator%s.add(%observator%);
	}

	public void remove%observated%(%observator% %observator%Object) {
    		%observator%s.remove(%observator%);
    	}

    %informInfo%
	public void %informobservator%() {
		Iterator<%observator%> it = %observator%s.iterator();
		while(it.hasNext())
		{
			it.next().update();
		}
	}

	public %observatedState% get%observatedState%Object() {
		return %observatedState%Object;
	}
}